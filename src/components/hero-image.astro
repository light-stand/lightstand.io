---
import { Image } from "astro:assets";
import devsImage from "~/assets/devs.jpg";

const widths = [450, 800, 1200];
const sizes = "100vw";
---

<Image
  class="h-full w-full object-cover"
  src={devsImage}
  widths={widths}
  sizes={sizes}
  alt="Lightstand devs"
/>
